# Generated protobuf/gRPC stubs land here at build time (see Makefile:
# `make gen-python`). The Makefile patches the grpc stub's absolute import
# (`import inference_pb2`) into a package-relative one so `from gen import ...`
# works. Do not edit generated files by hand.
